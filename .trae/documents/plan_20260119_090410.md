# 实现计划

## 1. 添加HTML结构
- **位置**: 在"最新1期两球组合平均率最高统计期详情"卡片的表格下方
- **内容**: 添加一个新的div元素，用于显示推荐买号合集
- **样式**: 使用与页面风格一致的样式

## 2. 添加JavaScript功能
### 2.1 实现合集更新函数
- **函数名**: `updateBuyNumbersCollectionNewest`
- **功能**: 收集表格中"下一期前区推荐买号"列的所有红色球，使用Set确保每个球只显示一次
- **实现**: 
  - 获取表格tbody元素
  - 遍历表格行，收集所有红色球
  - 使用Set去重
  - 排序后生成HTML显示

### 2.2 集成到现有代码
- **初始化调用**: 在页面加载完成后调用一次，确保初始状态正确
- **回测完成后调用**: 在`performSearchNewest`函数末尾添加调用，确保合集数据与表格数据同步

## 3. 样式设计
- **容器样式**: 使用与页面其他卡片一致的背景色和边框
- **标题样式**: 使用h4标签，与页面其他标题一致
- **球的样式**: 复用现有的`.red-ball`样式

## 4. 具体实现步骤
1. 在`xia_qi_kai_jiang_yu_ce.html`文件中，找到"最新1期两球组合平均率最高统计期详情"卡片
2. 在表格下方添加合集显示的HTML结构
3. 在JavaScript代码中添加`updateBuyNumbersCollectionNewest`函数
4. 在页面初始化时调用该函数
5. 在回测完成后调用该函数

## 5. 预期效果
- 初始状态: 显示"暂无推荐买号数据"
- 回测完成后: 显示所有下一期前区推荐买号的合集，每个球只显示一次，按数字顺序排列