## 问题分析
当前 `drawBuyNumberChart` 函数的实现中，横轴显示的是次数，柱状图上显示的是球号，而用户希望横轴显示球号，柱状图上显示次数。

## 修复方案
1. **修改横轴显示**：将横轴上的次数改为球号
2. **修改柱状图显示**：将柱状图上的球号改为次数
3. **调整文字位置**：确保文字显示在合适的位置

## 修复步骤
1. 打开 `xia_qi_hou_qu_kai_jiang_yu_ce.html` 文件
2. 定位到 `drawBuyNumberChart` 函数
3. 修改横轴文字显示：将 `item.count.toString()` 改为 `item.number.toString().padStart(2, '0')`
4. 修改柱状图文字显示：将 `item.number.toString().padStart(2, '0')` 改为 `item.count.toString()`
5. 调整文字位置，确保显示效果良好
6. 测试修复效果

## 预期结果
- 横轴显示球号
- 柱状图上显示次数
- 图表显示清晰，符合用户需求