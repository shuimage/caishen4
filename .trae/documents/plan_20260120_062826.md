经过详细分析，我发现xia_qi_hou_qu_kai_jiang_bi_sha.html文件中5个后区卡片的推荐买号获取函数实现方式存在很大差异，这是导致回测完成后出现很多空数据的原因。

### 问题分析
1. **最新1期卡片**：使用`huo_qu_hou_qu_tui_jian_bi_sha_hao`函数，调用`shuang_sha_fen_xi`接口，正确处理了`backCombinations`可能在不同位置的情况
2. **倒数2期卡片**：使用`huo_qu_hou_qu_tui_jian_mai_hao2`函数，调用`dao_shu_2_qi_liang_qiu_zu_he`接口，但只检查了`analysisData.backCombinations`
3. **倒数3期卡片**：使用`huo_qu_hou_qu_tui_jian_mai_hao3`函数，调用`dao_shu_3_qi_liang_qiu_zu_he`接口，存在多个问题：
   - 注释说"遍历前区组合"，但应该是遍历后区组合
   - 访问`analysisData.combinations`而不是`analysisData.backCombinations`
   - 检查`combo.nextDrawNumbers`而不是`combo.numberCounts`
   - 使用两位格式号码进行比较，可能与实际数据不符
4. **其他期数**：可能存在类似问题

### 解决方案
统一修改所有5个后区卡片的推荐买号获取函数，确保它们：
1. 使用与最新1期相同的逻辑，从回测结果中计算推荐买号
2. 确保正确处理接口返回的数据结构
3. 统一处理不同期数的特殊情况
4. 确保返回的推荐买号格式一致

### 修改步骤
1. 检查`huo_qu_hou_qu_tui_jian_mai_hao2`、`huo_qu_hou_qu_tui_jian_mai_hao3`、`huo_qu_hou_qu_tui_jian_mai_hao4`和`huo_qu_hou_qu_tui_jian_mai_hao5`函数
2. 将它们修改为使用与`huo_qu_hou_qu_tui_jian_bi_sha_hao`类似的实现方式
3. 确保正确访问接口返回的数据结构
4. 统一推荐买号的计算逻辑
5. 测试修改后的效果

通过这些修改，我将确保所有5个后区卡片的推荐买号都能正确显示，不会出现空数据的情况。