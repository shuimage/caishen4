## 修复页面乱码问题计划

### 1. 问题分析
- 发现文件中每个字符前面都有一个"围"字，导致页面显示乱码
- 这是因为之前的修复脚本出现问题，将每个字符错误地处理了
- 例如：`<html>` 变成了 `围<围h围t围m围l围>`

### 2. 修复策略
- **第一步：移除错误添加的"围"字符**
  - 创建新脚本，移除所有文件中的"围"字符
  - 这将恢复文件的原始状态

- **第二步：正确修复原始乱码问题**
  - 重新创建修复脚本，正确处理乱码字符
  - 确保脚本不会影响正常字符
  - 只替换真正的乱码字符 `�`，不替换其他字符

### 3. 修复步骤

#### 3.1 移除错误添加的"围"字符
- 编写一个脚本，遍历所有HTML文件
- 移除每个文件中的所有"围"字符
- 保存修复后的文件

#### 3.2 修复原始乱码问题
- 重新创建修复脚本，使用正确的替换映射
- 只替换 `�` 字符，不替换其他字符
- 使用正确的编码读取和写入文件

#### 3.3 验证修复结果
- 检查修复后的文件是否正常显示
- 测试API调用是否正常
- 验证页面是否能正确显示数据

### 4. 修复范围
- 所有在之前修复中受影响的HTML文件
- 重点关注用户提到的三个页面：
  - `zu_he_xiang_qing.html`
  - `dao_shu_2_qi_liang_qiu_zu_he_xiang_qing.html`
  - `dao_shu_3_qi_liang_qiu_zu_he_xiang_qing.html`

### 5. 预期结果
- 页面不再显示"围"字符
- 原始乱码问题得到修复
- 页面能正常显示API返回的数据

### 6. 风险控制
- 先备份原始文件，防止修复失败
- 分批次修复，每批次修复后验证
- 确保脚本不会影响其他正常字符

### 7. 工具准备
- Node.js脚本用于移除"围"字符
- Node.js脚本用于修复原始乱码问题
- 浏览器用于验证修复结果