# 修改计划：实现倒数2期两球组合前区买号回测结果页面

## 1. 页面创建与结构设计
- 创建或修改 `d:\中啦\caishen4\qian\dao_shu_2_qi_mai_hao_hui_ce_xiang_qing.html` 页面
- 参考 `sha_hao_mai_hao_hui_ce_xiang_qing.html` 页面结构，保持相同的布局和样式
- 修改页面标题为"倒数2期两球组合前区买号回测结果"
- 调整返回按钮的链接，指向 `dao_shu_2_qi_liang_qiu_zu_he.html`

## 2. API调用配置
- 配置页面调用 `d:\中啦\caishen4\hou\倒数2期两球前区买号回测.js` 接口
- 接口路径：`http://localhost:18889/dao_shu_2_qi_mai_hao_hui_ce`
- 确保API请求参数与后端接口匹配：`backtest_period`、`stats_period`、`backtest_method`

## 3. 前区推荐买号算法实现
- 实现与 `dao_shu_2_qi_liang_qiu_zu_he.html` 页面相同的前区推荐买号算法
- 算法包括三种类型：
  - 出现最多球：找到totalCounts中的最大值对应的号码
  - 出现最少球：找到totalCounts中除0以外的最小值对应的号码
  - 出现平均球：找到totalCounts中次数等于平均值的号码

## 4. 回测结果渲染
- 修改 `renderBacktestData` 函数，适配后端返回的数据结构
- 注意：后端返回的是下下期开奖结果，不是下期开奖结果
- 表格列调整为：序号、当前回测期号、前区推荐买号、推荐买号数量、下下期开奖期号、下下期实际开奖前区、前区正确买号数、前区错误买号数、是否成功买号
- 计算并展示平均正确率

## 5. 页面跳转处理
- 确保 `dao_shu_2_qi_liang_qiu_zu_he.html` 页面中的"前区推荐买号"卡片的回测按钮能够正确跳转到该页面
- 传递正确的参数：`backtest_period`、`stats_period`

## 6. 参数处理与用户交互
- 支持从URL参数获取回测期数、统计期数和回测方法
- 将用户选择的参数保存到localStorage
- 实现回测方法切换功能（出现最多/最少/平均）
- 实现回测期数和统计期数的选择

## 7. 主题切换支持
- 实现主题切换功能，从localStorage读取主题设置
- 确保页面元素在不同主题下都能正常显示

## 8. 修改文件
- `d:\中啦\caishen4\qian\dao_shu_2_qi_mai_hao_hui_ce_xiang_qing.html`：创建或修改页面，实现所有功能

## 9. 预期效果
- 页面能够正确调用 `d:\中啦\caishen4\hou\倒数2期两球前区买号回测.js` 接口
- 页面能够正确渲染回测结果，包括下下期开奖结果
- 页面支持不同的回测周期、统计周期和回测方法
- 页面能够从 `dao_shu_2_qi_liang_qiu_zu_he.html` 页面正确跳转
- 页面支持主题切换

## 10. 测试计划
- 访问页面，检查是否能正确加载回测数据
- 测试不同的回测方法，确保算法正确
- 测试不同的回测周期和统计周期，确保数据正确
- 测试从 `dao_shu_2_qi_liang_qiu_zu_he.html` 页面跳转，确保参数传递正确
- 测试主题切换，确保页面元素显示正常

通过以上修改，将实现一个功能完整、算法正确的倒数2期两球组合前区买号回测结果页面，能够满足用户的回测需求。