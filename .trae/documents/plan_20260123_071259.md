# 修复下一期前区推荐买号无数据问题

## 问题分析

当前页面`sha_qiu_ping_jun_lv_zui_di_detail.html`中的"下一期前区推荐买号"无数据，原因如下：

1. **接口调用错误**：`huo_qu_qian_qu_tui_jian_mai_hao`函数调用了错误的接口`huo_qu_sha_hao_hui_ce`
2. **数据格式不符**：获取的数据格式与预期不一致，导致无法正确计算推荐买号
3. **算法实现差异**：虽然算法逻辑与`sha_hao_fen_xi.html`相似，但缺少正确的数据来源

## 解决方案

### 1. 确认正确的接口
需要使用与`sha_hao_fen_xi.html`相同的接口获取最新1期两球组合数据

### 2. 修复数据获取逻辑
修改`huo_qu_qian_qu_tui_jian_mai_hao`函数：
- 调用正确的接口获取最新1期两球组合数据
- 确保获取到的数据格式与预期一致
- 正确处理接口返回的数据

### 3. 确保算法一致性
- 保持与`sha_hao_fen_xi.html`完全相同的算法
- 正确初始化和计算`totalCounts`数组
- 根据回测方法选择对应的推荐买号

## 具体修改

1. **修改huo_qu_qian_qu_tui_jian_mai_hao函数**
   - 调用正确的接口获取最新1期两球组合数据
   - 确保获取到的数据格式与预期一致
   - 正确计算推荐买号

2. **测试修改结果**
   - 确保回测完成后能正确显示下一期前区推荐买号
   - 确保推荐买号与`sha_hao_fen_xi.html`的算法结果一致

## 预期效果

- 回测完成后，"下一期前区推荐买号"显示正确的数据
- 推荐买号算法与`sha_hao_fen_xi.html`完全相同
- 支持三种回测方法：出现最多、出现最少、出现平均