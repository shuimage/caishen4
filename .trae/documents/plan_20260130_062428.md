# 创建 sha_hao_hou_ping_jun_lv_zui_gao_detail.html 页面

## 需求分析
- 创建新页面 `sha_hao_hou_ping_jun_lv_zui_gao_detail.html`
- 从 `sha_hao_hou_mai_hao_hui_ce_xiang_qing.html` 页面点击"平均率最高"按钮进入
- 参考 `sha_hao_hou_ping_jun_lv_zui_di_detail.html` 页面的结构
- 区别是这是平均率最高，而不是最低
- 在"最新1期两球组合后区平均率最高统计期详情"卡片的"下一期后区推荐买号"的算法规则要跟 `sha_hao_fen_xi.html` 页面的"后区推荐买号"卡片的算法一致

## 实现计划

### 1. 复制参考页面结构
- 复制 `sha_hao_hou_ping_jun_lv_zui_di_detail.html` 页面的基本结构到新文件 `sha_hao_hou_ping_jun_lv_zui_gao_detail.html`
- 修改页面标题和所有相关文本内容，将"最低"改为"最高"

### 2. 修改搜索逻辑
- 修改 `performSearch` 函数，从搜索平均率最低的统计期改为搜索平均率最高的统计期
- 修改 `testStatsPeriod` 函数，确保它正确计算平均正确率

### 3. 实现算法
- 修改 `huo_qu_hou_qu_tui_jian_mai_hao` 函数
- 实现与 `sha_hao_fen_xi.html` 页面相同的后区推荐买号算法
- 算法步骤：
  1. 从 `shuang_sha_fen_xi` 接口获取后区组合数据
  2. 统计每个后区号码的出现次数
  3. 根据回测方法（出现最多、出现最少、出现平均）计算推荐买号

### 4. 更新页面内容
- 修改页面标题为"最新1期两球组合后区平均率最高统计期详情"
- 更新所有卡片标题和相关文本，将"最低"改为"最高"
- 确保页面导航和返回按钮正常工作

### 5. 测试和验证
- 确保页面可以正常加载
- 测试API调用是否正常
- 验证算法是否正确实现
- 确保页面在不同主题下显示正常

## 关键技术点
- 修改搜索逻辑，从搜索平均率最低的统计期改为搜索平均率最高的统计期
- 实现与 `sha_hao_fen_xi.html` 页面相同的后区推荐买号算法
- 保持与参考页面相同的整体结构和用户体验
- 确保代码风格和命名约定与现有代码一致