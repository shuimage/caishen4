# 实现倒数4期两球组合平均率最低统计期详情卡片功能

## 任务目标
修改 `xia_qi_qian_qu_kai_jiang_bi_sha.html` 文件中的"倒数4期两球组合平均率最低统计期详情"卡片，使其功能与 `dao_shu_4_qi_ping_jun_lv_zui_di_detail.html` 文件中的对应卡片功能相同，并添加数据过滤逻辑。

## 实现步骤

### 1. 添加核心函数
- **`dao_shu_4_qi_mai_hao_hui_ce()`**：从后端获取倒数4期两球回测数据
- **`testStatsPeriod4()`**：测试单个统计期值的平均正确率（倒数4期）
- **`huo_qu_qian_qu_tui_jian_mai_hao_fourth_last()`**：获取倒数4期两球前区推荐买号
- **`getLastPeriod4()`**：获取最后一期期号（倒数4期两球专用）
- **`renderDetailData4()`**：渲染倒数4期详情数据，包含数据过滤逻辑
- **`performSearch4()`**：执行倒数4期两球搜索并显示进度

### 2. 实现数据过滤逻辑
在 `renderDetailData4()` 函数中添加数据过滤逻辑：
- 如果平均正确率相同，下下下下期后区推荐买号的值也相同，则任意显示一条
- 如果平均正确率相同，下下下下期后区推荐买号的值不同，则都要显示

### 3. 添加事件监听器
为倒数4期卡片的开始回测按钮添加点击事件监听器，调用 `performSearch4()` 函数。

### 4. 测试功能
确保前端服务器正常运行，测试倒数4期卡片的功能是否正常工作。

## 技术参考
- 参考 `dao_shu_4_qi_ping_jun_lv_zui_di_detail.html` 文件中的功能实现
- 参考 `xia_qi_qian_qu_kai_jiang_bi_sha.html` 文件中"最新期两球组合前区平均率最低统计期详情"卡片的数据过滤方法
- 确保代码风格与现有代码保持一致

## 预期结果
- "倒数4期两球组合平均率最低统计期详情"卡片功能与参考页面相同
- 回测完成后，数据按照要求进行过滤显示
- 页面功能正常，无语法错误