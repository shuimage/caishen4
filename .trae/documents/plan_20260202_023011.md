# 修改xia_qi_hou_qu_kai_jiang_bi_sha.js实现倒数2期卡片功能

## 目标
修改"倒数2期两球组合后区平均率最低统计期详情"卡片的功能，使其与dao_shu_2_qi_hou_ping_jun_lv_zui_di_detail.html页面的对应卡片功能一致，并添加数据过滤逻辑。

## 实现步骤

### 1. 添加必要的函数
- `huo_qu_dao_shu_2_qi_hou_mai_hao_hui_ce()` - 从后端获取倒数2期后区回测数据
- `testStatsPeriod2()` - 测试倒数2期单个统计期值的平均正确率
- `huo_qu_hou_qu_tui_jian_bi_sha_hao2()` - 获取倒数2期后区推荐杀号
- `renderDetailData2()` - 渲染倒数2期详情数据
- `performSearch2()` - 执行倒数2期搜索并显示进度

### 2. 实现数据过滤逻辑
- 在`performSearch2()`函数中，添加数据过滤逻辑
- 过滤规则：
  - 如果平均正确率相同，下下期后区推荐买号的值也相同，则任意显示一条
  - 如果平均正确率相同，下下期后区推荐买号的值不同，则都要显示

### 3. 添加事件监听器
- 在DOMContentLoaded事件中添加开始回测按钮2的点击事件监听器

### 4. 确保函数的正确性
- 检查所有新增函数的实现
- 确保函数能够正确获取数据和渲染结果

## 技术要点
- 使用JavaScript的数组方法（如reduce、filter、map）实现数据过滤
- 确保异步操作的正确处理
- 保持代码风格与现有代码一致
- 确保功能与参考页面一致

## 预期结果
修改后的"倒数2期两球组合后区平均率最低统计期详情"卡片功能将与dao_shu_2_qi_hou_ping_jun_lv_zui_di_detail.html页面的对应卡片功能一致，并且在回测完成后会按照要求对数据进行过滤。