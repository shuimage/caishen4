## 前区回测逻辑分析

**sha_qiu_ping_jun_lv_zui_gao_detail.html**页面的"最新1期两球组合平均率最高统计期详情"卡片的回测逻辑：

1. **回测方法**：使用排名方法（rank1到rank30）
2. **搜索策略**：多阶段搜索，步长从100→50→10
3. **过滤方法**：如果回测方法相同且下一期前区推荐买号相同，只保留一条
4. **推荐买号**：根据排名选择号码，计算每个号码的实际排名，找出对应排名的号码

## 后区回测页修改计划

修改**sha_hao_hou_ping_jun_lv_zui_gao_detail.html**页面，参考前区的回测逻辑：

### 1. 修改`huo_qu_hou_qu_tui_jian_mai_hao`函数
- 添加排名方法的支持
- 实现与前区相同的排名计算逻辑
- 为后区号码（1-12）计算排名并返回对应排名的号码

### 2. 修改`performSearch`函数
- 修改回测方法列表，使用排名方法（rank1到rank12）
- 调整搜索策略，与前区保持一致（步长100→50→10）
- 添加与前区相同的过滤方法

### 3. 修改`renderDetailData`函数
- 添加对排名方法的显示支持
- 显示"排名第X"的回测方法名称

### 4. 其他调整
- 确保后区号码范围正确（1-12）
- 保持与前区相同的UI显示风格

## 具体修改步骤

1. **修改`huo_qu_hou_qu_tui_jian_mai_hao`函数**：
   - 添加排名方法的处理逻辑
   - 实现号码排名计算
   - 返回对应排名的后区号码

2. **修改`performSearch`函数**：
   - 替换回测方法列表为排名方法（rank1到rank12）
   - 调整搜索策略参数
   - 添加结果过滤逻辑

3. **修改`renderDetailData`函数**：
   - 添加排名方法的中文名称显示

4. **测试验证**：
   - 执行回测确保排名方法正常工作
   - 验证过滤方法是否正确
   - 检查结果显示是否符合预期

## 预期结果

修改后，后区回测页将：
- 支持排名第1到第12的回测方法
- 使用与前区相同的搜索策略和过滤方法
- 显示与前区一致的回测结果
- 提供更全面的后区号码推荐分析