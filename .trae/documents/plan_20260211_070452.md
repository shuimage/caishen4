# 后区页面回测结果排序功能实现

## 问题分析
用户要求在 `sha_hao_hou_ping_jun_lv_zui_gao_detail.html` 页面的回测完成后，对回测结果数据按“平均正确率”进行降序排序。

## 代码分析
1. **数据流向**：
   - `performSearch` 函数执行回测并获取结果
   - 为每个结果获取后区推荐买号，存储在 `result.backBuyNumbers` 中
   - 进行去重过滤
   - 过滤掉空推荐买号
   - 调用 `renderDetailData` 函数渲染结果

2. **最佳排序位置**：
   在 `performSearch` 函数中，过滤空推荐买号之后，渲染结果之前，添加排序逻辑。

## 实现步骤
1. **修改 `performSearch` 函数**：
   - 在过滤空推荐买号之后，添加排序逻辑
   - 按 `accuracy` 字段进行降序排序
   - 确保平均正确率高的结果排在前面

2. **验证修改**：
   - 确保修改后的代码逻辑正确
   - 确保排序功能只影响显示顺序，不影响其他功能

## 具体修改位置
文件：`d:\中啦\caishen4\qian\sha_hao_hou_ping_jun_lv_zui_gao_detail.html`
函数：`performSearch`
位置：在过滤空推荐买号之后，渲染结果之前（约第 882 行之后）