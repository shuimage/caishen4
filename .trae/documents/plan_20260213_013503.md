## 移动X期数设置到倒数第X期开奖信息卡片

### 任务描述
将 `dao_shu_x_qi_liang_qiu_zu_he.html` 页面中X期数设置的输入框从页面顶部移动到“倒数第X期开奖信息”卡片中，并确保输入X后相应的数据同步更新。

### 修改步骤

1. **移除当前的X期数设置区域**
   - 从页面顶部移除 `<div class="x-period-input">` 区域（包含输入框和确定按钮）

2. **在“倒数第X期开奖信息”卡片中添加X期数设置**
   - 在卡片的标题 `<h3 style="margin: 0;">倒数第X期开奖信息</h3>` 下方添加X期数输入框和确定按钮
   - 保持输入框ID为 `xPeriod`，确保现有的JavaScript函数仍然有效
   - 保持按钮的 `onclick="updateXPeriod()"` 事件处理
   - 添加适当的样式，确保与卡片风格一致

3. **验证JavaScript函数**
   - 确保 `updateXPeriod()` 函数仍然能够正确更新X期数并重新加载数据
   - 确保 `xuan_ran_dao_shu_x_qi_kai_jiang()` 函数能够正确获取并显示倒数第X期的开奖信息
   - 确保 `renderFirstZoneAnalysisData()` 和 `renderSecondZoneAnalysisData()` 函数能够正确更新分析数据

### 预期结果
- X期数设置的输入框出现在“倒数第X期开奖信息”卡片中
- 输入X后点击确定按钮，页面会重新加载数据，显示对应倒数X期的分析结果
- 页面布局美观合理，用户体验良好