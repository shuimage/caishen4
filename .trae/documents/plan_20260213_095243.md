## 修复计划

### 问题分析
`xia_qi_hou_qu_kai_jiang_yu_ce.html` 页面的 "最新一期两球组合后区平均率最高统计期详情" 卡片与 `sha_hao_hou_ping_jun_lv_zui_gao_detail.html` 页面的对应卡片存在差异，主要体现在数据处理和排序逻辑上。

### 解决方案
修改 `xia_qi_hou_qu_kai_jiang_yu_ce.js` 文件中的 `renderDetailDataNewestBack` 函数，添加与 `sha_hao_hou_ping_jun_lv_zui_gao_detail.html` 页面相同的结果去重和按平均正确率降序排序的逻辑。

### 修改步骤
1. 打开 `d:\中啦\caishen4\qian\js\xia_qi_hou_qu_kai_jiang_yu_ce.js` 文件
2. 找到 `renderDetailDataNewestBack` 函数（第726-812行）
3. 修改函数实现，添加以下逻辑：
   - 过滤回测结果：如果回测方法相同，正确率也相同，下一期后区推荐买号的值也相同，则只显示一个
   - 过滤掉下一期后区推荐买号为空的结果
   - 按平均正确率降序排序结果
4. 保存文件并测试功能

### 预期结果
修改后，`xia_qi_hou_qu_kai_jiang_yu_ce.html` 页面的 "最新一期两球组合后区平均率最高统计期详情" 卡片在回测完成后，将按照平均正确率从高到低排序显示结果，与 `sha_hao_hou_ping_jun_lv_zui_gao_detail.html` 页面的对应卡片行为一致。