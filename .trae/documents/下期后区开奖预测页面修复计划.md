## 修复计划

### 问题1：倒数2-5期开奖信息卡片未正确显示数据
**原因**：`renderDrawInfo`函数中访问了错误的字段名，期号字段应为`issue`而非`period`
**修复方法**：
1. 修改`renderDrawInfo`函数，将`drawInfo.period`改为`drawInfo.issue`
2. 检查其他字段名是否正确，确保数据能正确渲染

### 问题2：去掉"推荐买号统计"卡片下的"最新1期开奖信息"卡片
**原因**：页面中存在额外的最新开奖信息卡片
**修复方法**：
1. 删除ID为`latestDrawInfoSectionNewestBack`的div元素
2. 移除相关的JavaScript代码

### 问题3：修改"倒数2期两球组合后区平均率最高统计期详情"卡片功能
**原因**：当前使用模拟数据，功能与目标页面不一致
**修复方法**：
1. 更新表格结构，将`combination-table`类改为`detail-list`类
2. 实现与`dao_shu_2_qi_hou_ping_jun_lv_zui_gao_detail.html`相同的搜索逻辑：
   - 添加`huo_qu_dao_shu_2_qi_hou_mai_hao_hui_ce`函数调用正确的API
   - 实现多阶段搜索策略
   - 添加`testStatsPeriod`函数测试统计期值
   - 实现`renderDetailData`函数渲染结果
   - 添加复制功能
3. 更新按钮事件处理，使用新的搜索逻辑

## 实施步骤

1. 修复`renderDrawInfo`函数中的字段名错误
2. 删除额外的最新开奖信息卡片
3. 更新倒数2期卡片的HTML结构
4. 添加必要的JavaScript函数：
   - `huo_qu_dao_shu_2_qi_hou_mai_hao_hui_ce`
   - 适用于倒数2期的`testStatsPeriod`函数
   - 适用于倒数2期的`performSearch`函数
   - 适用于倒数2期的`renderDetailData`函数
   - 适用于倒数2期的`huo_qu_hou_qu_tui_jian_mai_hao`函数
5. 更新按钮事件监听器

## 预期效果

1. 倒数2-5期开奖信息卡片能正确显示数据
2. 页面结构更清晰，无冗余卡片
3. 倒数2期卡片功能与目标页面完全一致，支持真实API调用和复杂搜索逻辑