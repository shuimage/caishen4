### 实现计划

#### 1. 页面修改
- **在"总回测控制"区域添加两个新按钮**：
  - 在"复制推荐买号"按钮右侧添加"保存回测结果"按钮，ID为`saveBacktestResultsBtn`
  - 在"保存回测结果"按钮右侧添加"加载回测结果"按钮，ID为`loadBacktestResultsBtn`
  - 按钮样式参考`xia_qi_kai_jiang_yu_ce.html`的实现

#### 2. JavaScript功能实现

##### 2.1 按钮状态管理
- **保存回测结果按钮**：初始状态为禁用，回测完成后启用
- **加载回测结果按钮**：始终启用，点击后加载结果

##### 2.2 保存回测结果功能
- **创建`bao_cun_hui_ce_jie_guo_quan_bu`函数**：
  - 收集所有卡片的回测结果
  - 调用后端API保存结果到数据库
  - 显示保存成功/失败提示

##### 2.3 加载回测结果功能
- **创建`jia_zai_hui_ce_jie_guo`函数**：
  - 从数据库加载当前回测期的回测结果
  - 根据卡片类型调用对应的渲染函数
  - 显示加载成功/失败提示

##### 2.4 辅助函数
- **创建`collectAllBacktestResults`函数**：收集所有卡片的回测结果
- **创建`updateSaveButtonStatus`函数**：更新保存按钮的状态

#### 3. 卡片配置
- **定义卡片配置对象**：包含每个卡片的ID、渲染函数、期号计算规则等
- **配置示例**：
  ```javascript
  const cardConfigs = {
    '最新1期两球组合后区': {
      tbodyId: 'detailResultsNewestBack',
      renderFunction: renderDetailDataNewestBack,
      nextPeriodPrefix: ''
    },
    // 其他卡片配置...
  };
  ```

#### 4. 事件监听
- **为新按钮添加点击事件监听器**
- **在回测完成后调用`updateSaveButtonStatus`函数**

#### 5. 样式和主题适配
- **确保新按钮适配深色主题**
- **使用与现有按钮一致的样式类**

### 技术细节

1. **API调用**：使用现有的`bao_cun_hui_ce_jie_guo`和`huo_qu_hui_ce_jie_guo`接口
2. **期号计算**：根据卡片类型计算当前回测期号和各种下期期号
3. **数据收集**：遍历每个卡片的表格，收集回测结果数据
4. **状态管理**：使用全局变量跟踪回测状态，控制按钮禁用状态
5. **错误处理**：添加适当的错误处理和用户提示

### 预期效果

- 页面加载后，"保存回测结果"按钮为禁用状态
- 所有回测完成后，"保存回测结果"按钮变为启用状态
- 点击"保存回测结果"按钮，将所有卡片的回测结果保存到数据库
- 点击"加载回测结果"按钮，从数据库加载当前回测期的回测结果并渲染到页面
- 所有操作都有适当的用户提示

### 代码参考
- 参考`xia_qi_kai_jiang_yu_ce.html`中对应的按钮实现
- 参考现有回测函数的结构和逻辑
- 复用现有的API调用函数

### 实现步骤
1. 修改HTML，添加两个新按钮
2. 实现保存回测结果功能
3. 实现加载回测结果功能
4. 实现按钮状态管理
5. 添加事件监听
6. 测试功能完整性

### 文件修改
- **主要修改文件**：`d:/中啦/caishen4/qian/xia_qi_hou_qu_kai_jiang_yu_ce.html`
- **涉及的后端API**：
  - `http://localhost:18889/bao_cun_hui_ce_jie_guo`（保存回测结果）
  - `http://localhost:18889/huo_qu_hui_ce_jie_guo`（加载回测结果）

### 注意事项
- 确保与现有代码风格保持一致
- 遵循现有的API调用模式
- 添加适当的错误处理和用户反馈
- 确保按钮状态正确反映回测进度
- 适配深色主题样式