## 修复计划

### 1. 修改表格表头
- 将"最新1期两球组合后区平均率最高统计期详情"卡片中表格的"当前期号"改为"当前回测期号"
- 在"当前回测期号"后面增加"下一期期号"字段

### 2. 修改回测结果渲染逻辑
- 实现只显示平均率最高的结果
- 当有多条平均率最高相同的数据时，判断"下一期后区推荐买号"是否相同
- 如果推荐买号相同，则随机留下一条
- 如果有多个相同平均率最高的结果且推荐买号不同，则分别随机各保留一条

### 3. 实现期号计算逻辑
- 获取最新1期的期号作为"当前回测期号"
- 计算最新1期的下一期期号，用于"下一期期号"字段

### 4. 具体修改点

#### 4.1 HTML结构修改
- 修改表格表头，更新列名和添加新列
- 文件位置：`xia_qi_hou_qu_kai_jiang_yu_ce.html` 第438-448行

#### 4.2 JavaScript逻辑修改
- 修改 `renderDetailData` 函数，实现结果筛选和显示逻辑
- 添加期号递增函数，用于计算下一期期号
- 文件位置：`xia_qi_hou_qu_kai_jiang_yu_ce.html` 第1053-1108行

### 5. 修复步骤

1. **修改表格表头**
   - 将第444行的`<th>当前期号</th>`修改为`<th>当前回测期号</th>`
   - 在第444行后添加`<th>下一期期号</th>`

2. **实现期号计算函数**
   ```javascript
   // 计算下一期期号
   function calculateNextPeriod(currentPeriod) {
     const periodNum = parseInt(currentPeriod.replace('期', ''));
     return `${periodNum + 1}期`;
   }
   ```

3. **修改renderDetailData函数**
   - 添加结果筛选逻辑，只保留平均率最高的结果
   - 实现按推荐买号去重的逻辑
   - 更新表格行渲染，显示"当前回测期号"和"下一期期号"

4. **测试修复效果**
   - 确保回测结果只显示平均率最高的一条或多条（根据推荐买号不同）
   - 确保"当前回测期号"和"下一期期号"正确显示
   - 确保点击"开始回测"后功能正常

### 6. 预期效果
- 表格列名正确修改，新增"下一期期号"字段
- 回测结果只显示平均率最高的记录
- 当有多个相同平均率的结果时，按照推荐买号去重
- 期号显示正确，"下一期期号"为"当前回测期号"的下一期