## 修复计划

### 问题分析
在`xia_qi_hou_qu_kai_jiang_yu_ce.html`页面的“倒数2期”和“倒数3期”两球组合后区平均率最高统计期详情卡片中，回测完成后返回的数据出现错位。具体原因是：

1. **表格列数不匹配**：
   - 两个卡片的表头都定义了8列（回测方法、回测期数、统计期数、平均正确率、当前回测期号、下下期期号、下下期后区推荐买号、操作）
   - 但渲染函数生成的表格行只包含7列，缺少了“当前回测期号”这一列

2. **具体问题函数**：
   - `renderDetailDataSecondLast`函数（第1666-1722行）：生成倒数2期卡片的表格行，缺少“当前回测期号”列
   - `renderDetailDataThirdLast`函数（第2192-2248行）：生成倒数3期卡片的表格行，缺少“当前回测期号”列

### 修复方案

1. **修改`renderDetailDataSecondLast`函数**：
   - 确保生成的表格行包含8列
   - 添加“当前回测期号”列
   - 正确计算和显示下下期期号

2. **修改`renderDetailDataThirdLast`函数**：
   - 确保生成的表格行包含8列
   - 添加“当前回测期号”列
   - 正确计算和显示下下下期期号

3. **验证修复**：
   - 确保两个卡片的表格数据不再错位
   - 所有字段都能正确显示
   - 按钮功能正常

### 修复步骤

1. **修改`renderDetailDataSecondLast`函数**：
   - 更新表格行HTML模板，添加“当前回测期号”列
   - 正确计算当前回测期号和下下期期号

2. **修改`renderDetailDataThirdLast`函数**：
   - 更新表格行HTML模板，添加“当前回测期号”列
   - 正确计算当前回测期号和下下下期期号

3. **测试修复效果**：
   - 运行页面，执行回测
   - 检查表格数据是否正确显示
   - 验证所有字段对齐正确

### 预期效果
- 两个卡片的表格数据不再错位
- 所有8列数据都能正确显示
- 下下期期号、下下期后区推荐买号和操作字段对齐正确
- 回测功能正常，数据显示完整