## 修复计划

### 1. HTML结构修改
- **更新表格样式**：将当前的 `combination-table` 改为 `detail-list` 类，匹配目标页面样式
- **调整表格列**：修改为目标页面的列结构（回测方法、回测期数、统计期数、平均正确率、当前期号、下一期后区推荐买号、操作）
- **添加开奖信息卡片**：在回测结果列表前添加最新1期开奖信息卡片
- **添加复制按钮**：为每行结果添加复制按钮

### 2. JavaScript功能实现
- **API调用函数**：
  - `getTotalPeriods()`：获取全部期数
  - `huo_qu_sha_hao_hou_mai_hao_hui_ce()`：获取回测数据
  - `huo_qu_zui_xin_kai_jiang()`：获取最新开奖信息
  - `huo_qu_hou_qu_tui_jian_mai_hao()`：获取后区推荐买号
- **回测逻辑**：
  - 实现多阶段搜索策略（从大到小变化步长）
  - 支持三种回测方法（出现最多、出现最少、出现平均）
  - 实现详细的进度显示
  - 添加终止回测功能
- **辅助功能**：
  - `copyToClipboard()`：复制推荐买号到剪贴板
  - `renderLatestDrawInfo()`：渲染最新开奖信息
  - `renderDetailData()`：渲染回测结果

### 3. 样式更新
- **完善深色主题**：添加完整的深色主题样式支持
- **更新表格样式**：使用目标页面的表格样式
- **增大球号显示**：更新球号样式为更大的尺寸
- **美化进度条**：更新进度条样式使其更美观

### 4. 数据处理与渲染
- 从URL参数获取回测方法（默认为average）
- 实现多阶段搜索算法，找到平均率最高的统计期
- 为每个结果获取对应的后区推荐买号
- 渲染结果时显示详细信息和复制按钮

### 5. 交互优化
- 实时更新进度条和状态信息
- 添加终止回测按钮功能
- 完善错误处理和用户提示

## 实施步骤

1. 先更新HTML结构，添加必要的DOM元素
2. 实现API调用函数和数据处理逻辑
3. 实现回测主函数和进度显示
4. 更新样式和主题支持
5. 测试功能完整性和用户交互

## 预期效果

修复后，当前页面的"最新1期两球组合后区平均率最高统计期详情"卡片将与目标页面具有完全一致的功能和样式，包括真实API调用、多阶段搜索、详细的进度显示、复制功能和完整的深色主题支持。