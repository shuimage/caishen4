## 修复计划

### 问题分析
在 `xia_qi_kai_jiang_yu_ce.js` 文件中，`renderDetailDataFifthLast` 函数负责渲染"倒数5期两球组合平均率最高统计期详情"卡片的内容，但它缺少了按照"平均正确率"进行降序排序的逻辑。

### 解决方案
修改 `renderDetailDataFifthLast` 函数，添加与其他卡片相同的去重和排序逻辑：

1. **添加结果去重处理**：使用 `Map` 对象过滤重复的回测结果，确保相同回测方法和推荐买号的结果只显示一次
2. **添加排序逻辑**：在渲染结果前，按照"平均正确率"（accuracy字段）进行降序排序，确保正确率高的结果排在前面

### 修改步骤
1. 打开 `d:\中啦\caishen4\qian\js\xia_qi_kai_jiang_yu_ce.js` 文件
2. 找到 `renderDetailDataFifthLast` 函数（第2380-2459行）
3. 在函数中添加结果去重逻辑，使用 `Map` 对象过滤重复结果
4. 在去重后添加排序逻辑，按照 `accuracy` 字段降序排序
5. 修改渲染循环，使用排序后的结果数组
6. 保存文件并测试功能

### 预期结果
修复后，"倒数5期两球组合平均率最高统计期详情"卡片在回测完成后，将按照"平均正确率"从高到低排序显示结果，与其他卡片的行为一致。