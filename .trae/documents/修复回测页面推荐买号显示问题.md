### 修复计划

**问题分析：**
1. **最新1期卡片**：字段名不匹配（`backBuyNumbers` vs `frontBuyNumbers`）
2. **倒数3期卡片**：`updateProgress`函数仍在使用`requestAnimationFrame`，导致浏览器不可见时回测暂停
3. **所有卡片**：需要确保API数据处理逻辑正确

**修复步骤：**

1. **修复最新1期卡片字段名不匹配**
   - 修改`performSearch`函数中的字段名，将`backBuyNumbers`改为`frontBuyNumbers`
   - 确保`renderDetailData`函数能正确获取推荐买号

2. **修复倒数3期卡片的requestAnimationFrame问题**
   - 将`performSearch3`函数中的`updateProgress`函数修改为直接DOM操作，移除`requestAnimationFrame`
   - 修改相关的进度显示代码，确保不依赖动画帧

3. **检查并修复所有卡片的API数据处理逻辑**
   - 确保`huo_qu_hou_qu_tui_jian_bi_sha_hao`、`huo_qu_dao_shu_2_qi_qian_qu_tui_jian_mai_hao`和`huo_qu_dao_shu_3_qi_qian_qu_tui_jian_mai_hao`函数能正确返回数据
   - 检查数据处理逻辑，确保能正确统计和计算推荐买号

4. **统一所有卡片的代码风格**
   - 确保所有卡片的代码结构一致
   - 统一使用直接DOM操作更新进度，移除所有`requestAnimationFrame`依赖

**修复文件：**
- `d:/中啦/caishen4/qian/xia_qi_qian_qu_kai_jiang_bi_sha.html`

**预期效果：**
- 所有卡片在回测完成后能正确显示前区推荐买号
- 回测过程在浏览器不可见时能正常执行
- 进度显示正常，不依赖动画帧

**测试验证：**
- 打开页面，执行回测
- 检查所有卡片的推荐买号字段是否有球显示
- 切换浏览器标签页，验证回测仍在继续执行
- 检查控制台是否有错误信息