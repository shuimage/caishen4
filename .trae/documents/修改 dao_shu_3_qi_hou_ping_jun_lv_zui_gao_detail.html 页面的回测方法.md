# 修改 dao_shu_3_qi_hou_ping_jun_lv_zui_gao_detail.html 页面的回测方法

## 分析现有逻辑

### dao_shu_3_qi_ping_jun_lv_zui_gao_detail.html 的处理逻辑
1. 使用 `dao_shu_3_qi_mai_hao_hui_ce` 函数从后端获取回测数据
2. 使用 `testStatsPeriod` 函数测试单个统计期值的平均正确率
3. 采用多阶段搜索策略（步长从200到50）寻找平均率最高的统计期
4. 支持多种回测方法，包括 "出现最多"、"出现最少"、"出现平均" 和排名方法（rank1到rank30）
5. 对于排名方法，根据号码出现次数排序并计算排名，选择指定排名的号码
6. 使用并行测试提高效率

### dao_shu_3_qi_liang_qiu_zu_he.html 的后区排名规则
1. 统计每个后区号码（1-12）的出现次数
2. 按出现次数降序排序号码
3. 计算每个号码的实际排名（考虑并列情况）
4. 根据排名选择号码

## 修改计划

### 1. 修改 huo_qu_hou_qu_tui_jian_mai_hao 函数
- 添加对排名方法的支持
- 实现与 dao_shu_3_qi_liang_qiu_zu_he.html 一致的排名规则
- 按出现次数排序后区号码并计算排名
- 根据指定排名选择号码

### 2. 修改 performSearch 函数
- 移除 "出现最多"、"出现最少"、"出现平均" 三种回测方法
- 添加排名方法（rank1到rank12）
- 修改搜索起始期为1500期
- 确保使用顺序测试（非并行）
- 保持多阶段搜索策略的效率

### 3. 修改 renderDetailData 函数
- 添加对排名方法的显示支持
- 显示排名方法为 "排名第X"

### 4. 其他修改
- 确保 "下下下期后区推荐买号" 的算法规则与 dao_shu_3_qi_liang_qiu_zu_he.html 一致
- 过滤掉没有找到对应排名号码的结果
- 保持与前区页面相似的用户体验

## 技术要点
- 保持代码结构清晰，与现有代码风格一致
- 确保排名规则的正确实现，特别是并列情况的处理
- 优化搜索策略，确保从1500期开始的回测效率
- 确保不使用并行测试，符合用户要求
- 保持与其他页面的一致性，特别是推荐买号的算法逻辑