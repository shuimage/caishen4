# 修改 xia_qi_qian_qu_kai_jiang_bi_sha.html 页面的倒数2期卡片功能

## 任务分析
- 修改 `xia_qi_qian_qu_kai_jiang_bi_sha.html` 页面的 "倒数2期两球组合平均率最低统计期详情" 卡片功能
- 使其与 `dao_shu_2_qi_ping_jun_lv_zui_di_detail.html` 页面的对应卡片功能相同
- 增加数据过滤功能：
  - 如果平均正确率相同，下下期后区推荐买号的值也相同，则任意显示一条
  - 如果平均正确率相同，下下期后区推荐买号的值不同，则都要显示

## 实现计划

### 1. 分析现有代码
- 查看 `xia_qi_qian_qu_kai_jiang_bi_sha.html` 页面的完整 JavaScript 代码
- 找到 "最新期两球组合前区平均率最低统计期详情" 卡片的过滤方法
- 分析 `dao_shu_2_qi_ping_jun_lv_zui_di_detail.html` 页面的实现

### 2. 实现倒数2期卡片的功能
- 添加 `performSearch2()` 函数，用于执行倒数2期的回测
- 添加 `renderDetailData2()` 函数，用于渲染倒数2期的回测结果
- 添加 `huo_qu_hou_qu_tui_jian_mai_hao_second_last()` 函数，用于获取后区推荐买号
- 添加 `testStatsPeriod2()` 函数，用于测试单个统计期的平均正确率

### 3. 实现数据过滤功能
- 在 `renderDetailData2()` 函数中添加数据过滤逻辑
- 参考最新期卡片的过滤方法
- 实现以下过滤规则：
  - 如果平均正确率相同，下下期后区推荐买号的值也相同，则任意显示一条
  - 如果平均正确率相同，下下期后区推荐买号的值不同，则都要显示

### 4. 添加事件监听器
- 为倒数2期卡片的 "开始回测" 按钮添加点击事件监听器
- 调用 `performSearch2()` 函数

### 5. 测试功能
- 确保回测功能正常工作
- 确保数据过滤功能正确实现
- 确保页面显示正常

## 技术要点
- 使用 async/await 处理异步操作
- 实现多阶段搜索策略，从大到小变化步长
- 使用 Map 对期数进行去重处理
- 实现数据过滤逻辑，基于平均正确率和后区推荐买号
- 确保代码风格与现有代码一致
- 确保错误处理和用户反馈