# 修改"倒数2期两球组合后区平均率最高统计期详情"卡片功能

## 任务目标
将`xia_qi_hou_qu_kai_jiang_yu_ce.html`页面中的"倒数2期两球组合后区平均率最高统计期详情"卡片的功能修改为与`dao_shu_2_qi_hou_ping_jun_lv_zui_gao_detail.html`页面中的同名卡片功能相同，同时保持字段不变。

## 核心功能修改

### 1. 修改回测方法
- 将当前使用的回测方法（most, least, average）替换为排名方法（rank1到rank12）
- 确保排名方法的实现与`dao_shu_2_qi_liang_qiu_zu_he.html`的"后区两球组合统计"卡片中的排名规则一致

### 2. 修改搜索策略
- 将搜索起始期数从20期修改为2000期
- 实现多阶段搜索策略，寻找平均率最高的统计期
- 调整搜索阶段和步长，确保与文档说明中描述的功能一致

### 3. 修改后区推荐买号算法
- 确保后区推荐买号算法与`dao_shu_2_qi_liang_qiu_zu_he.html`的"后区两球组合统计"卡片中的排名规则一致
- 支持排名方法（rank1到rank12）的推荐买号计算

### 4. 保持字段结构不变
- 确保修改后的功能保持`xia_qi_hou_qu_kai_jiang_yu_ce.html`文件中的字段结构不变
- 保持表格列的顺序和名称不变

## 实现步骤

### 1. 修改`performSearchSecondLastBack`函数
- 更新回测方法数组，使用排名方法（rank1到rank12）
- 修改搜索起始期数为2000期
- 调整搜索阶段和步长
- 确保使用正确的后区推荐买号算法

### 2. 修改`renderDetailDataSecondLastBack`函数
- 更新回测方法中文名称的显示，支持排名方法（rank1到rank12）
- 确保表格渲染逻辑与字段结构保持一致

### 3. 测试修改后的功能
- 确保修改后的功能能够正常运行
- 验证回测结果的准确性
- 确保排名方法的实现正确

## 技术要点

### 排名方法实现
- 排名方法（rank1到rank12）的实现应与`dao_shu_2_qi_liang_qiu_zu_he.html`的"后区两球组合统计"卡片中的排名规则一致
- 排名方法应基于后区号码的出现次数进行排序，并为每个号码分配排名

### 多阶段搜索策略
- 实现多阶段搜索策略，从大到小变化步长
- 确保搜索范围的扩展和缩小逻辑正确

### 后区推荐买号算法
- 确保后区推荐买号算法能够根据排名方法正确计算推荐号码
- 支持排名方法（rank1到rank12）的推荐买号计算

## 预期结果

修改后的"倒数2期两球组合后区平均率最高统计期详情"卡片应：
- 支持排名方法回测（rank1到rank12）
- 从2000期开始搜索平均率最高的统计期
- 显示下下期后区推荐买号
- 提供回测进度显示和控制功能
- 支持复制推荐号码到剪贴板
- 保持字段结构不变

这样，该卡片的功能就与`dao_shu_2_qi_hou_ping_jun_lv_zui_gao_detail.html`页面中的同名卡片功能相同了。