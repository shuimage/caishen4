# 修改dao_shu_3_qi_liang_qiu_zu_he和dao_shu_3_qi_hou_qu_zu_he接口统计逻辑

## 1. 后端修改

### 1.1 修改倒数3期两球组合前区.js

**修改点：**
- **遍历历史数据循环**：将下下期索引 `i-2` 改为下下下期索引 `i-3`
- **变量名更新**：将 `nextNextDraw` 改为 `nextNextNextDraw`
- **注释更新**：将所有"下下期"改为"下下下期"
- **occurrenceDetails字段**：
  - 将 `nextNextPeriod` 改为 `nextNextNextPeriod`
  - 将 `nextNextDrawDate` 改为 `nextNextNextDrawDate`
  - 将 `nextNextFrontNumbers` 改为 `nextNextNextFrontNumbers`
- **推荐杀号计算**：更新注释，将"下下期"改为"下下下期"

**具体修改位置：**
- 第116-119行：遍历循环和变量定义
- 第149-159行：获取下下下期号码
- 第168-176行：记录组合出现详细信息
- 第184-217行：推荐杀号计算相关注释

### 1.2 修改倒数3期两球组合后区.js

**修改点：**
- **遍历历史数据循环**：将下下期索引 `i-2` 改为下下下期索引 `i-3`
- **变量名更新**：将 `nextNextDraw` 改为 `nextNextNextDraw`
- **注释更新**：将所有"下下期"改为"下下下期"
- **occurrenceDetails字段**：
  - 将 `nextNextPeriod` 改为 `nextNextNextPeriod`
  - 将 `nextNextDrawDate` 改为 `nextNextNextDrawDate`
  - 将 `nextNextBackNumbers` 改为 `nextNextNextBackNumbers`

**具体修改位置：**
- 第116-119行：遍历循环和变量定义
- 第149-159行：获取下下下期号码
- 第168-176行：记录组合出现详细信息

## 2. 前端修改

### 2.1 修改dao_shu_3_qi_liang_qiu_zu_he.html

**修改点：**
- **API调用**：确保调用正确的接口
- **数据处理**：更新字段名，将"nextNextPeriod"等改为"nextNextNextPeriod"等
- **页面显示**：将"下下期"改为"下下下期"
- **注释更新**：将所有"下下期"改为"下下下期"

**具体修改位置：**
- JavaScript部分：处理API返回数据的逻辑
- HTML部分：显示"下下期"的文本

### 2.2 检查其他引用页面

**需要检查的页面：**
- sha_hao_fen_xi.html
- dao_shu_2_qi_liang_qiu_zu_he.html
- san_qiu_fen_xi.html
- dao_shu_2_qi_san_qiu_zu_he.html
- dao_shu_2_qi_liang_qiu_zu_he_xiang_qing.html
- san_qiu_zu_he_xiang_qing.html
- dao_shu_2_qi_san_qiu_zu_he_xiang_qing.html

**修改内容：**
- 检查是否有引用这两个接口的代码
- 如果有，更新字段名和注释

## 3. 测试验证

**测试步骤：**
1. 重启后端服务器，确保新代码生效
2. 访问前端页面，检查API调用是否成功
3. 检查页面显示是否正确，"下下期"已改为"下下下期"
4. 检查统计数据是否符合预期，基于下下下期计算
5. 测试不同统计周期参数，确保接口正常工作

## 4. 注意事项

1. **索引边界处理**：确保遍历循环的起始索引正确，避免数组越界
2. **字段名一致性**：确保前后端字段名完全一致
3. **注释更新**：确保所有相关注释都已更新，便于后续维护
4. **测试覆盖**：确保测试覆盖不同统计周期和数据情况
5. **向后兼容**：如果有其他接口或页面依赖这些字段，需要同步更新

## 5. 预期效果

- 两个接口现在统计的是组合出现时下下下期的号码情况
- 前端页面正确显示"下下下期"数据
- 所有相关字段名和注释都已更新
- 接口功能正常，返回数据符合预期

## 6. 风险评估

- **低风险**：修改逻辑简单，主要是索引和字段名的变更
- **中风险**：如果有其他未检查到的页面引用这些接口，可能会出现错误
- **应对措施**：仔细检查所有相关页面，确保没有遗漏

## 7. 依赖关系

- 后端修改依赖数据库配置和查询逻辑
- 前端修改依赖后端返回的字段名
- 测试依赖前后端都已正确修改

## 8. 质量保证

- 代码修改后进行全面测试
- 检查所有相关页面的功能
- 确保注释和文档与代码一致
- 遵循现有代码风格和命名规范

## 9. 交付标准

- 两个接口的统计逻辑已从下下期改为下下下期
- 前端参数和显示已同步更新
- 所有相关页面功能正常
- 代码注释和文档已更新
- 测试通过，无明显错误