# 修改dao_shu_5_qi_hou_ping_jun_lv_zui_gao_detail.html文件的回测方法

## 分析现状
- 当前文件使用的回测方法包括：出现最多、出现最少、出现平均
- 当前从20期开始回测
- 没有实现排名方法
- 没有过滤重复结果的逻辑

## 实现计划

### 1. 修改回测方法配置
- 移除现有的三种方法（most, least, average）
- 添加排名方法，从rank1到rank12（对应后区1-12个排名）

### 2. 修改统计期范围
- 将回测起始期从20期改为1500期
- 保持多阶段搜索策略，但调整步长和扩展范围

### 3. 实现后区排名规则
- 修改`huo_qu_hou_qu_tui_jian_mai_hao`函数
- 添加排名计算逻辑，与dao_shu_5_qi_liang_qiu_zu_he.html中的后区排名规则一致
- 支持根据排名获取对应的后区号码

### 4. 修改搜索策略
- 保持顺序回测（非并行）
- 调整搜索阶段的步长和扩展范围
- 确保从1500期开始搜索

### 5. 添加结果过滤功能
- 在回测完成后，添加过滤逻辑
- 当回测方法、正确率、推荐买号都相同时，只保留一个结果

### 6. 添加排序功能
- 在过滤后，按平均正确率进行降序排序

### 7. 更新渲染逻辑
- 修改`renderDetailData`函数，支持显示排名方法
- 更新表格显示，确保排名方法正确显示为"排名第X"

### 8. 测试验证
- 确保修改后的代码能正常运行
- 验证排名规则是否与dao_shu_5_qi_liang_qiu_zu_he.html一致
- 验证过滤和排序功能是否正常工作

## 技术要点
- 使用与dao_shu_5_qi_liang_qiu_zu_he.html相同的排名算法
- 保持代码风格与现有代码一致
- 确保接口调用正确，失败时显示"接口失败"提示
- 保持响应式的进度显示

## 预期结果
- 页面能够从1500期开始回测
- 只使用排名方法进行回测
- 推荐买号的算法规则与dao_shu_5_qi_liang_qiu_zu_he.html一致
- 回测结果经过过滤和排序
- 界面显示正确的排名方法和推荐号码