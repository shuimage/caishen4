# 修改xia_qi_hou_qu_kai_jiang_bi_sha.js实现数据过滤功能

## 目标
修改"最近期两球组合后区平均率最低统计期详情"卡片的功能，使其与sha_hao_hou_ping_jun_lv_zui_di_detail.html页面的对应卡片功能一致，并添加数据过滤逻辑。

## 实现步骤

### 1. 修改performSearch()函数
- 在获取所有结果后，添加数据过滤逻辑
- 过滤规则：
  - 如果平均正确率相同，下一期后区推荐买号的值也相同，则任意显示一条
  - 如果平均正确率相同，下一期后区推荐买号的值不同，则都要显示

### 2. 实现数据过滤逻辑
- 按平均正确率对结果进行分组
- 对每个正确率组，再按推荐买号的值进行分组
- 对于每个推荐买号值组，只保留一条结果
- 合并所有保留的结果

### 3. 确保huo_qu_hou_qu_tui_jian_bi_sha_hao()函数正确获取推荐杀号
- 检查函数实现是否正确
- 确保函数能够根据回测方法计算对应的后区推荐买号

### 4. 确保renderDetailData()函数正确渲染结果
- 检查函数实现是否正确
- 确保函数能够渲染过滤后的结果

## 技术要点
- 使用JavaScript的数组方法（如reduce、filter、map）实现数据过滤
- 确保异步操作的正确处理
- 保持代码风格与现有代码一致
- 确保功能与参考页面一致

## 预期结果
修改后的卡片功能将与sha_hao_hou_ping_jun_lv_zui_di_detail.html页面的对应卡片功能一致，并且在回测完成后会按照要求对数据进行过滤。