# 任务：修改倒数2期后区平均率最高统计期详情页面的回测方法

## 分析现状
1. **已有的前区回测页面**：`dao_shu_2_qi_ping_jun_lv_zui_gao_detail.html` 实现了前区的回测逻辑，包括多阶段搜索策略和排名方法
2. **参考的排名规则**：`dao_shu_2_qi_liang_qiu_zu_he.html` 中的"后区两球组合统计"卡片实现了后区号码的排名规则
3. **目标文件**：需要创建或修改 `dao_shu_2_qi_hou_ping_jun_lv_zui_gao_detail.html` 文件

## 实现计划

### 步骤1：创建/修改目标文件
- 基于 `dao_shu_2_qi_ping_jun_lv_zui_gao_detail.html` 创建 `dao_shu_2_qi_hou_ping_jun_lv_zui_gao_detail.html`
- 修改页面标题和相关文本为后区相关
- 更新API调用路径为后区相关接口

### 步骤2：实现后区推荐买号函数
- 创建 `huo_qu_hou_qu_tui_jian_mai_hao` 函数，参考 `dao_shu_2_qi_liang_qiu_zu_he.html` 中的后区排名规则
- 实现逻辑：
  1. 初始化后区汇总计数数组（1-12对应后区号码）
  2. 遍历后区组合，统计每个号码的出现次数
  3. 按出现次数降序排序号码
  4. 计算每个号码的实际排名（考虑并列情况）
  5. 根据排名方法选择对应排名的号码

### 步骤3：修改回测逻辑
- 修改 `performSearch` 函数，使其：
  1. 支持后区的排名方法（rank1到rank12）
  2. 调用 `huo_qu_hou_qu_tui_jian_mai_hao` 函数获取后区推荐买号
  3. 过滤掉空推荐号码的排名方法结果
  4. 对结果进行去重处理
  5. 按平均正确率降序排序

### 步骤4：更新API调用
- 修改 `dao_shu_2_qi_mai_hao_hui_ce` 函数，调用后区相关的API
- 确保API参数正确传递

### 步骤5：测试和验证
- 启动服务并访问页面
- 点击"开始回测"按钮，验证回测功能是否正常
- 检查"下下期后区推荐买号"是否按照排名规则生成
- 验证过滤和排序功能是否正常

### 步骤6：更新文档说明
- 在"文档说明.md"中添加或更新该页面的核心功能
- 包括页面的导航栏、功能、样式等
- 添加接口的核心功能、参数、返回值、使用示例等

## 技术要点
- 多阶段搜索策略：从大到小变化步长，提高搜索效率
- 并行处理：每次处理多个统计期，提高回测速度
- 排名规则：根据出现次数计算排名，支持并列情况
- 过滤和去重：确保结果的有效性和唯一性
- 响应式UI：支持深色主题，提供良好的用户体验