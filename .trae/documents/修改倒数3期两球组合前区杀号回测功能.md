# 修改倒数3期两球组合前区杀号回测功能

## 1. 修改后端文件 `dao_shu_3_qi_sha_hao_hui_ce.js`
- 更新 `getDaoShu3QiKillNumberBacktest` 函数，使其接受 `backtest_period` 和 `stats_period` 两个参数
- 更新 SQL 查询，使用 `backtest_period` 获取历史数据用于回测，使用 `stats_period` 获取统计数据用于计算杀号
- 更新路由处理函数，接受这两个参数
- 更新返回结果，包含这两个参数

## 2. 修改前端文件 `dao_shu_3_qi_sha_hao_hui_ce_xiang_qing.html`
- 在页面顶部添加 "回测期数" 和 "统计期数" 下拉框
- 添加获取 URL 参数的函数 `getURLParameters()`
- 修改 `huo_qu_dao_shu_3_qi_sha_hao_hui_ce` 函数，使其接受 `backtest_period` 和 `stats_period` 两个参数
- 修改 `fetchAndRenderBacktestData` 函数，使其使用这两个参数调用 API
- 添加事件监听器，当下拉框值改变时，重新获取数据
- 修改页面初始化代码，从 URL 参数中获取初始值

## 3. 修改前端文件 `dao_shu_3_qi_liang_qiu_zu_he.html`
- 更新 "查看回测结果" 链接的 `href` 属性，使其传递 `backtest_period=50` 和当前选择的统计范围作为 `stats_period`

## 4. 测试
- 确保从 `dao_shu_3_qi_liang_qiu_zu_he.html` 点击 "查看回测结果" 能正确跳转到回测详情页
- 确保回测详情页的下拉框能正确影响数据加载
- 确保回测期数改变后，回测结果列表数据跟着改变
- 确保统计期数改变后，推荐杀号算法跟着改变

这个计划将确保所有功能都按照用户要求实现，并且保持与之前修改的其他回测页面的一致性。