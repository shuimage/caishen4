## 修改计划

### 1. 修改 `performSearchFourthLastBack` 函数
- **更新回测方法**：将回测方法从 `['most', 'least', 'average']` 修改为只使用排名方法（rank1到rank12）
- **更新搜索起始期数**：将搜索起始期数从20修改为1500
- **更新搜索阶段设置**：修改搜索阶段的步长和扩展范围，与参考页面一致
  ```javascript
  const searchStages = [
    { step: 200, expansion: 400 }, // 第一阶段：步长200，扩展范围400
    { step: 100, expansion: 200 }, // 第二阶段：步长100，扩展范围200
    { step: 50, expansion: 100 }    // 第三阶段：步长50，扩展范围100
  ];
  ```
- **更新推荐买号获取**：修改获取后区推荐买号的调用，使用支持排名方法的逻辑

### 2. 修改 `renderDetailDataFourthLastBack` 函数
- **更新回测方法显示**：添加对排名方法中文名称的支持，显示为"排名第X"
- **更新过滤逻辑**：修改结果过滤逻辑，与参考页面一致
- **更新排序方式**：按平均正确率降序排序结果

### 3. 修改 `huo_qu_hou_qu_tui_jian_mai_hao_fourth_last` 函数
- **添加排名方法支持**：添加对排名方法的处理逻辑，与dao_shu_4_qi_liang_qiu_zu_he.html中的算法一致
- **更新数据处理**：确保正确处理接口返回的数据结构，统计每个号码的出现次数
- **实现排名计算**：根据号码出现次数计算排名，并返回对应排名的号码

### 4. 验证修改
- 确保修改后的功能与参考页面一致
- 保持原页面的字段不变
- 确保所有相关函数调用正确更新