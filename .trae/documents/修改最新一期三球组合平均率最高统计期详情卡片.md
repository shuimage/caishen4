# 修改计划

## 目标
修改 `xia_qi_kai_jiang_yu_ce.html` 页面中的 "最新一期三球组合平均率最高统计期详情" 卡片，使其与 `san_qiu_ping_jun_lv_zui_gao_detail.html` 中的 "最新1期三球组合平均率最高统计期详情" 卡片使用相同的回测方法和过滤功能，同时保留原有的列表字段。

## 需要修改的文件
1. **d:\中啦\caishen4\qian\js\xia_qi_kai_jiang_yu_ce.js**
   - 修改 `performSearchThreeBall` 函数
   - 修改 `huo_qu_qian_qu_tui_jian_mai_hao_three_ball` 函数
   - 修改 `renderDetailDataThreeBall` 函数

## 修改内容

### 1. 修改 `performSearchThreeBall` 函数
- 将搜索阶段从5个改为3个，步长分别为200、100、50
- 将回测方法从 ['most', 'least', 'average'] 改为使用排名方法 ['rank1' 到 'rank30']
- 将搜索起始期数从20期改为2000期
- 实现并行测试统计期的方法，提高搜索效率
- 添加过滤空推荐号码的排名方法结果的逻辑

### 2. 修改 `huo_qu_qian_qu_tui_jian_mai_hao_three_ball` 函数
- 添加对排名方法的支持，与 `san_qiu_ping_jun_lv_zui_gao_detail.html` 中的实现一致
- 实现根据排名选择号码的逻辑

### 3. 修改 `renderDetailDataThreeBall` 函数
- 添加对排名方法的显示支持，显示为 "排名第X"
- 添加过滤回测结果的逻辑，确保相同回测方法和推荐买号的结果只显示一条

## 注意事项
- 保持原有的列表字段不变
- 确保修改后的代码与原有的代码风格一致
- 确保修改后的代码能够正确处理错误情况
- 确保修改后的代码能够与其他部分的代码正常交互