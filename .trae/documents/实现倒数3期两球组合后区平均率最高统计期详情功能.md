# 实现倒数3期两球组合后区平均率最高统计期详情功能

## 实现步骤

### 1. 添加回测数据获取函数
- 添加 `huo_qu_dao_shu_3_qi_hou_mai_hao_hui_ce` 函数，调用后端接口获取倒数3期后区回测数据
- 实现错误处理，确保接口失败时给出提示

### 2. 添加统计期测试函数
- 添加 `testStatsPeriodThirdLastBack` 函数，测试单个统计期值的平均正确率
- 计算成功率并返回结果

### 3. 添加后区推荐买号获取函数
- 添加 `huo_qu_hou_qu_tui_jian_mai_hao_third_last` 函数，使用与 `dao_shu_3_qi_liang_qiu_zu_he.html` 相同的算法
- 调用 `dao_shu_3_qi_hou_qu_zu_he` 接口获取后区组合数据
- 实现三种回测方法的推荐买号计算：出现最多、出现最少、出现平均

### 4. 添加详情数据渲染函数
- 添加 `renderDetailDataThirdLastBack` 函数，渲染详情数据
- 实现数据过滤逻辑：如果平均正确率相同，下下下期后区推荐买号相同则任意显示一条，不同则都显示
- 格式化数据并生成表格行
- 添加复制到剪贴板功能

### 5. 添加回测执行函数
- 添加 `performSearchThirdLastBack` 函数，实现完整的回测流程
- 实现多阶段搜索策略，从大到小变化步长
- 显示详细的进度信息
- 支持终止回测功能
- 处理回测结果并调用渲染函数

### 6. 添加事件监听器
- 为 `startBacktestBtnThirdLastBack` 按钮添加点击事件，调用 `performSearchThirdLastBack` 函数

## 技术要点

- **API集成**：调用后端提供的倒数3期后区回测和组合数据接口
- **多阶段搜索**：使用从大到小的步长策略，提高搜索效率
- **数据过滤**：根据平均正确率和推荐买号进行智能过滤
- **用户体验**：显示详细的回测进度，支持终止操作
- **错误处理**：确保接口调用失败时给出明确提示
- **代码复用**：参考已实现的最新一期和倒数2期功能，保持代码结构一致

## 预期结果

修改完成后，"倒数3期两球组合后区平均率最高统计期详情" 卡片将具备与 `dao_shu_3_qi_hou_ping_jun_lv_zui_gao_detail.html` 页面相同的功能，包括：
- 点击开始回测按钮执行完整的回测流程
- 显示详细的回测进度和结果
- 对回测结果进行智能过滤
- 支持复制推荐买号到剪贴板
- 与页面其他卡片保持一致的用户体验