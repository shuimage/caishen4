# 实现倒数3期后区平均率最低统计期详情页面

## 任务目标
创建 `dao_shu_3_qi_hou_ping_jun_lv_zui_di_detail.html` 页面，参考 `sha_hao_hou_ping_jun_lv_zui_di_detail.html` 的结构，实现倒数3期后区平均率最低统计期详情功能。

## 实现步骤

### 1. 创建页面文件
- 复制 `sha_hao_hou_ping_jun_lv_zui_di_detail.html` 到 `dao_shu_3_qi_hou_ping_jun_lv_zui_di_detail.html`

### 2. 修改页面标题和内容
- 更新页面标题为 "倒数3期两球组合后区平均率最低统计期详情"
- 更新卡片标题为 "倒数3期两球组合后区平均率最低统计期详情"
- 更新表格列标题为 "下下下期后区推荐买号"

### 3. 修改 API 调用
- 将 `huo_qu_sha_hao_hou_mai_hao_hui_ce` 替换为 `huo_qu_dao_shu_3_qi_hou_mai_hao_hui_ce`
- 将 `shuang_sha_fen_xi` 接口替换为 `dao_shu_3_qi_hou_qu_zu_he` 接口

### 4. 实现后区推荐买号算法
- 复制 `dao_shu_3_qi_liang_qiu_zu_he.html` 中的后区推荐买号算法
- 实现出现最多球、出现最少球、出现平均球的计算逻辑
- 确保算法与 `dao_shu_3_qi_liang_qiu_zu_he.html` 完全一致

### 5. 更新页面渲染逻辑
- 修改 `getLatestPeriod` 函数，返回下下下期的期号
- 更新 `renderDetailData` 函数，确保显示后区数据
- 更新 `copyToClipboard` 函数，确保正确复制后区号码

### 6. 测试功能
- 确保页面能够正常加载
- 测试开始回测功能
- 测试搜索平均率最低的统计期功能
- 测试复制到剪贴板功能
- 确保页面能够正常返回上一页

## 技术要点

### 算法实现
```javascript
// 初始化汇总计数数组
const backTotalCounts = new Array(13).fill(0); // 索引0不使用，1-12对应后区号码

// 遍历后区组合，统计每个号码的出现次数
analysisData.combinations.forEach(combo => {
  // 累加每个号码的出现次数
  for (let i = 1; i <= 12; i++) {
    const count = combo.numberCounts ? (combo.numberCounts[i] || 0) : 0;
    backTotalCounts[i] += count;
  }
});

// 根据回测方法计算对应的后区推荐买号
if (backtestMethod === 'most') {
  // 1. 出现最多球：找到backTotalCounts中的最大值对应的号码
  const maxLastCount = Math.max(...backTotalCounts);
  if (maxLastCount > 0) {
    // 找出所有最大值对应的号码
    for (let i = 1; i <= 12; i++) {
      if (backTotalCounts[i] === maxLastCount) {
        backBuyNumbers.push(i);
      }
    }
  }
} else if (backtestMethod === 'least') {
  // 2. 出现最少球：找到backTotalCounts中除0以外的最小值对应的号码
  const nonZeroBackCounts = backTotalCounts.filter(count => count > 0);
  if (nonZeroBackCounts.length > 0) {
    const minLastCount = Math.min(...nonZeroBackCounts);
    // 找出所有最小值对应的号码
    for (let i = 1; i <= 12; i++) {
      if (backTotalCounts[i] === minLastCount) {
        backBuyNumbers.push(i);
      }
    }
  }
} else if (backtestMethod === 'average') {
  // 3. 出现平均球：找到backTotalCounts中次数等于平均值的号码
  const nonZeroTotalBackCounts = backTotalCounts.filter(count => count > 0);
  if (nonZeroTotalBackCounts.length > 0) {
    const sumBackCounts = nonZeroTotalBackCounts.reduce((sum, count) => sum + count, 0);
    const averageBackCount = Math.round(sumBackCounts / nonZeroTotalBackCounts.length);
    // 找出所有等于平均值的号码
    for (let i = 1; i <= 12; i++) {
      if (backTotalCounts[i] === averageBackCount) {
        backBuyNumbers.push(i);
      }
    }
  }
}
```

### API 调用
- 回测数据接口：`http://localhost:18889/huo_qu_dao_shu_3_qi_hou_mai_hao_hui_ce`
- 后区组合数据接口：`http://localhost:18889/dao_shu_3_qi_hou_qu_zu_he`
- 最新开奖信息接口：`http://localhost:18889/sql_zui_xin_yi_qi`

## 预期结果

创建完成后，`dao_shu_3_qi_hou_ping_jun_lv_zui_di_detail.html` 页面应该能够：
1. 正常加载并显示最新开奖信息
2. 执行回测并搜索平均率最低的统计期
3. 显示后区推荐买号，算法与 `dao_shu_3_qi_liang_qiu_zu_he.html` 一致
4. 提供复制到剪贴板功能
5. 能够正常返回上一页

这样就完成了用户要求的功能，确保了倒数3期后区平均率最低统计期详情页面的正常运行。