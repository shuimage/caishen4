# 实现倒数4期两球组合平均率最高统计期详情功能

## 任务目标
修改 `xia_qi_kai_jiang_yu_ce.html` 页面中的“倒数4期两球组合平均率最高统计期详情”卡片，集成 `dao_shu_4_qi_ping_jun_lv_zui_gao_detail.html` 中的功能，并添加结果过滤逻辑。

## 实现步骤

### 1. 添加辅助函数
- **`dao_shu_4_qi_mai_hao_hui_ce`**：从后端获取倒数4期的回测数据
- **`testStatsPeriodFourthLast`**：测试单个统计期值的平均正确率
- **`huo_qu_qian_qu_tui_jian_mai_hao_fourth_last`**：获取前区推荐买号
- **`renderDetailDataFourthLast`**：渲染详情数据并应用过滤逻辑

### 2. 替换 `performSearchFourthLast` 函数
- 实现与 `dao_shu_4_qi_ping_jun_lv_zui_gao_detail.html` 中相同的功能
- 添加多阶段搜索策略
- 实现进度可视化
- 添加错误处理
- 计算下下下下期期号（最新期号+4）

### 3. 添加结果过滤逻辑
- 在 `renderDetailDataFourthLast` 函数中实现过滤逻辑
- 确保回测方法、平均正确率和下下下下期前区推荐买号相同的记录只显示一条
- 使用 `Set` 数据结构来跟踪已见过的结果组合

### 4. 更新 `setupBacktestControls` 函数
- 添加对 `performSearchFourthLast` 函数的调用
- 确保点击“开始回测”按钮时能够正确触发回测功能

### 5. 测试功能
- 验证页面能够正确加载数据
- 验证回测功能能够正常执行
- 验证结果过滤逻辑能够正确工作
- 验证进度条能够正确显示

## 技术要点
- 使用与其他卡片相同的代码模式和命名约定
- 确保接口调用的一致性
- 实现完整的错误处理
- 保持代码结构清晰，易于维护
- 确保与现有功能的兼容性