# 实现倒数5期两球组合后区平均率最低统计期详情卡片功能

## 1. 添加必要的函数

### 1.1 获取倒数5期后区回测数据
- 添加 `huo_qu_dao_shu_5_qi_hou_mai_hao_hui_ce()` 函数
- 调用后端接口 `http://localhost:18889/huo_qu_dao_shu_5_qi_hou_mai_hao_hui_ce`
- 实现与其他期数类似的错误处理和数据返回逻辑

### 1.2 测试倒数5期单个统计期值的平均正确率
- 添加 `testStatsPeriod5()` 函数
- 调用 `huo_qu_dao_shu_5_qi_hou_mai_hao_hui_ce()` 获取回测数据
- 计算并返回平均正确率

### 1.3 获取倒数5期后区推荐杀号
- 添加 `huo_qu_hou_qu_tui_jian_bi_sha_hao5()` 函数
- 调用后端接口 `http://localhost:18889/dao_shu_5_qi_hou_qu_zu_he`
- 实现与其他期数类似的杀号计算逻辑

### 1.4 渲染倒数5期详情数据
- 添加 `renderDetailData5()` 函数
- 渲染到 `detailResults5` 元素
- 实现与其他期数类似的表格渲染逻辑

### 1.5 执行倒数5期搜索并显示进度
- 添加 `performSearch5()` 函数
- 实现多阶段搜索策略
- 添加数据过滤逻辑：
  - 按平均正确率对结果进行分组
  - 对每个正确率组，再按推荐买号的值进行分组
  - 如果平均正确率相同且推荐买号相同，只显示一条结果
  - 如果平均正确率相同但推荐买号不同，显示所有结果

## 2. 添加事件监听器
- 在 DOMContentLoaded 事件监听器中添加开始回测按钮5的点击事件监听器
- 绑定到 `performSearch5()` 函数

## 3. 保持代码一致性
- 遵循现有的命名约定和代码结构
- 实现与其他卡片相同的进度条功能
- 添加适当的错误处理

## 4. 验证功能
- 确保所有新增函数都能正常工作
- 验证数据过滤逻辑是否正确实现
- 确保页面能正常加载和运行