# 实现倒数5期两球组合后区平均率最高统计期详情卡片功能

## 实现步骤

### 1. 添加回测数据获取函数
- 函数名：`huo_qu_dao_shu_5_qi_hou_mai_hao_hui_ce`
- 功能：从后端获取倒数5期后区回测数据
- 调用接口：`http://localhost:18889/dao_shu_5_qi_hou_mai_hao_hui_ce`

### 2. 添加统计期测试函数
- 函数名：`testStatsPeriodFifthLastBack`
- 功能：测试单个统计期值的平均正确率
- 调用 `huo_qu_dao_shu_5_qi_hou_mai_hao_hui_ce` 获取回测数据并计算平均正确率

### 3. 添加后区推荐买号获取函数
- 函数名：`huo_qu_hou_qu_tui_jian_mai_hao_fifth_last`
- 功能：使用与 `dao_shu_5_qi_liang_qiu_zu_he.html` 相同的算法获取后区推荐买号
- 调用接口：`http://localhost:18889/dao_shu_5_qi_hou_qu_zu_he`

### 4. 添加详情数据渲染函数
- 函数名：`renderDetailDataFifthLastBack`
- 功能：渲染回测结果并实现数据过滤逻辑
- 过滤逻辑：如果平均正确率相同，下下下下下期后区推荐买号相同，则任意显示一条；如果平均正确率相同，下下下下下期后区推荐买号不同，则都要显示

### 5. 添加回测执行函数
- 函数名：`performSearchFifthLastBack`
- 功能：使用多阶段搜索策略，从大到小变化步长，找到平均率最高的统计期
- 搜索策略：步长从100期逐渐减小到5期
- 调用其他函数执行完整的回测流程

### 6. 添加事件监听器
- 为 `startBacktestBtnFifthLastBack` 按钮添加点击事件，触发 `performSearchFifthLastBack` 函数

### 7. 测试功能
- 通过代码检查确保功能正常工作
- 确保与 `dao_shu_5_qi_hou_ping_jun_lv_zui_gao_detail.html` 页面的对应卡片功能相同
- 确保数据过滤逻辑符合要求

## 技术实现要点

- 使用与其他期数卡片相同的代码结构和设计模式
- 调用专门的后端接口获取倒数5期两球组合数据
- 实现与参考页面相同的推荐买号算法
- 添加符合要求的数据过滤逻辑
- 确保代码风格与现有代码一致
- 提供良好的错误处理和用户反馈

## 预期结果

用户将能够在 `xia_qi_hou_qu_kai_jiang_yu_ce.html` 页面使用“倒数5期两球组合后区平均率最高统计期详情”卡片的回测功能，获取推荐买号，并看到符合要求的过滤结果。