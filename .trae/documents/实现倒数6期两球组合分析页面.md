# 实现“倒数X期两球组合分析”页面的计划

## 1. 前端实现

### 1.1 创建主页面 `dao_shu_x_qi_liang_qiu_zu_he.html`
- 基于 `dao_shu_5_qi_liang_qiu_zu_he.html` 文件复制并修改
- 添加 X 值输入/选择控件
- 动态更新页面标题为“倒数X期两球组合分析”
- 修改所有 API 调用，使用 X 值作为参数

### 1.2 创建子页面 `dao_shu_x_qi_liang_qiu_zu_he_xiang_qing.html`
- 基于 `dao_shu_5_qi_liang_qiu_zu_he_xiang_qing.html` 文件复制并修改
- 支持 X 值参数传递
- 修改所有 API 调用，使用 X 值作为参数

### 1.3 修改导航栏 `navigation.js`
- 在菜单栏中添加“倒数X期两球组合分析”链接

## 2. 后端实现

### 2.1 创建主接口 `dao_shu_x_qi_liang_qiu_zu_he.js`
- 基于 `dao_shu_5_qi_liang_qiu_zu_he.js` 文件复制并修改
- 添加 X 参数处理
- 动态计算倒数 X 期的起始期号
- 返回与原接口相同格式的数据

### 2.2 创建后区接口 `dao_shu_x_qi_hou_qu_zu_he.js`
- 基于 `dao_shu_5_qi_hou_qu_zu_he.js` 文件复制并修改
- 添加 X 参数处理
- 动态计算倒数 X 期的起始期号
- 返回与原接口相同格式的数据

### 2.3 创建详情接口 `dao_shu_x_qi_liang_qiu_zu_he_xiang_qing.js`
- 基于 `dao_shu_5_qi_liang_qiu_zu_he_xiang_qing.js` 文件复制并修改
- 添加 X 参数处理
- 动态计算倒数 X 期的起始期号
- 返回与原接口相同格式的数据

## 3. 功能特点

### 3.1 X 值选择
- 提供输入框让用户输入 X 值（1-10）
- 提供下拉菜单快速选择常用值
- 实时验证 X 值的有效性

### 3.2 数据加载
- 根据用户输入的 X 值动态加载数据
- 显示加载状态和错误提示
- 支持期数范围选择（最近100期、50期等）

### 3.3 页面布局
- 保持与原页面相同的布局结构
- 动态更新页面标题和相关文本
- 支持深色主题

### 3.4 交互功能
- 保持与原页面相同的交互功能
- 支持组合详情查看
- 支持杀号复制
- 支持图表排序和显示

## 4. 技术实现要点

### 4.1 前端
- 使用 URL 参数传递 X 值
- 使用 JavaScript 动态更新页面内容
- 使用 Fetch API 调用后端接口
- 使用 Canvas 绘制图表

### 4.2 后端
- 使用 Express.js 处理请求
- 使用 SQL 查询获取倒数 X 期的数据
- 保持与原接口相同的数据格式
- 实现错误处理和参数验证

## 5. 测试计划

- 测试不同 X 值（1-10）的功能
- 测试期数范围选择功能
- 测试详情页面跳转功能
- 测试杀号推荐功能
- 测试图表显示功能
- 测试错误处理功能