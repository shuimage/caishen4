# 实现冷热关联杀号算法的回测功能

## 1. 简化回测选项UI
- 移除回测方法和统计期数选项
- 只保留回测期数下拉选择框
- 与镜像对称杀号法回测页面保持一致的UI布局

## 2. 修改API调用函数
- 调整`huo_qu_huan_yuan_hui_ce_re_leng`函数，使用固定的stats_period=100
- 简化函数参数，只保留backtestPeriod和backtestMethod
- 使用默认的backtestMethod=most
- 确保调用正确的冷热关联杀号法回测接口

## 3. 更新数据渲染逻辑
- 修改`renderBacktestData`函数，使其能够处理与镜像对称杀号法回测相同的数据格式
- 使用`recommendedKillNumbers`字段代替`frontKillNumbers`
- 使用`period`和`nextPeriod`字段代替`currentIssue`和`nextIssue`
- 重新计算正确杀号数和错误杀号数

## 4. 调整页面加载逻辑
- 简化页面初始化过程，只处理回测期数
- 移除对回测方法和统计期数的处理
- 简化事件监听器，只保留回测期数下拉选择框的事件监听

## 5. 简化JavaScript函数
- 移除不需要的函数：`testStatsPeriod`、`getTotalPeriods`
- 修改`getURLParameters`函数，只返回backtestPeriod
- 修改`savePeriodSelection`和`getSavedPeriodSelection`函数，只处理backtestPeriod
- 简化平均率最低和最高功能，使用默认的backtest_method=most

## 6. 确保代码一致性
- 与镜像对称杀号法回测页面保持一致的代码结构
- 使用相同的变量命名和函数结构
- 确保页面样式和交互一致

## 7. 测试功能
- 确保页面能够正常加载和显示回测结果
- 确保回测期数选择功能正常工作
- 确保数据渲染正确
- 确保API调用正确

通过以上修改，我将实现冷热关联杀号算法的回测功能，使其与镜像对称杀号法回测页面保持一致的实现方式和列表展示数据，同时使用正确的冷热关联杀号法算法。