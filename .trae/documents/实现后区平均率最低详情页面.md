# 实现后区平均率最低详情页面

## 目标
创建 `sha_hao_hou_ping_jun_lv_zui_di_detail.html` 页面，实现点击 `sha_hao_hou_mai_hao_hui_ce_xiang_qing.html` 页面中的"平均率最低"按钮后跳转到该页面，并使用与 `sha_hao_fen_xi.html` 相同的后区推荐买号算法。

## 实现步骤

### 1. 创建新页面
- 复制 `sha_qiu_ping_jun_lv_zui_di_detail.html` 页面作为基础
- 修改页面标题为"最新1期两球组合后区平均率最低统计期详情"
- 更新页面内容，使其专注于后区数据

### 2. 实现后区推荐买号算法
- 在新页面的JavaScript中实现 `huo_qu_hou_qu_tui_jian_mai_hao` 函数
- 使用与 `sha_hao_fen_xi.html` 相同的算法：
  1. **出现最多球**：找到后区汇总计数中的最大值对应的号码
  2. **出现最少球**：找到后区汇总计数中除0以外的最小值对应的号码
  3. **出现平均球**：找到后区汇总计数中次数等于平均值的号码

### 3. 修改API调用
- 修改 `performSearch` 函数，使其调用后区相关的API：
  - 使用 `huo_qu_sha_hao_hou_mai_hao_hui_ce` 替代 `huo_qu_sha_hao_mai_hao_hui_ce`
  - 使用后区相关的参数和返回值

### 4. 更新页面渲染
- 修改 `renderDetailData` 函数，使其显示后区推荐买号：
  - 更新表格列，将"前区推荐买号"改为"后区推荐买号"
  - 使用蓝色球样式显示后区号码

### 5. 测试功能
- 点击 `sha_hao_hou_mai_hao_hui_ce_xiang_qing.html` 页面中的"平均率最低"按钮
- 验证是否成功跳转到新页面
- 点击"开始回测"按钮，验证搜索功能是否正常
- 验证后区推荐买号算法是否与 `sha_hao_fen_xi.html` 一致

## 技术要点
- 复制并修改现有页面结构，确保布局一致
- 实现与 `sha_hao_fen_xi.html` 相同的后区推荐买号算法
- 修改API调用，确保使用后区相关的接口
- 更新页面渲染逻辑，确保显示后区数据
- 测试功能，确保所有流程正常运行