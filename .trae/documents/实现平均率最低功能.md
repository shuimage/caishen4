# 实现平均率最低功能

## 1. 修改 `sha_hao_hou_mai_hao_hui_ce_xiang_qing.html`
- 在"平均率最高"按钮右侧添加"平均率最低"按钮
- 为新按钮添加点击事件，跳转到新页面 `sha_hao_hou_ping_jun_lv_zui_di_detail.html`

## 2. 创建新页面 `sha_hao_hou_ping_jun_lv_zui_di_detail.html`
- 复制 `sha_hao_hou_ping_jun_lv_zui_gao_detail.html` 的内容
- 修改页面标题为"最新1期两球组合后区平均率最低统计期详情"
- 修改页面主标题文本

## 3. 修改新页面的搜索逻辑
- 将 `performSearch` 函数中的搜索逻辑从"最高平均率"改为"最低平均率"
  - 将 `Math.max` 替换为 `Math.min` 来查找最低正确率
  - 将 `highestAccuracy` 变量名改为 `lowestAccuracy`
  - 将 `highestResultsInStage` 变量名改为 `lowestResultsInStage`
  - 将 `highestPeriods` 变量名改为 `lowestPeriods`

## 4. 调整起始期数
- 将搜索起始期数从30期改为50期
- 修改所有相关的注释和变量名

## 5. 保持其他功能不变
- 保持后区推荐买号算法不变
- 保持进度显示和用户交互不变
- 保持主题切换和响应式设计不变