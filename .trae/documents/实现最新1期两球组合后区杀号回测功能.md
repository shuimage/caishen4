# 实现最新1期两球组合后区杀号回测功能

## 1. 创建新的前端页面 `sha_hao_hui_ce_hou_xiang_qing.html`
- 复制 `sha_hao_hui_ce_xiang_qing.html` 页面，修改标题为"最新1期两球组合后区杀号回测结果"
- 修改表格列，将前区相关内容改为后区相关内容
- 回测结果列表包含：当前回测期号、后区推荐杀号、下期开奖期号、下期实际开奖后区、正确杀号数量、错误杀号数量
- 添加"回测期数"和"统计期数"下拉框
- 修改 API 调用函数，使其调用后区回测接口
- 添加事件监听器，当下拉框值改变时，重新获取数据

## 2. 修改前端页面 `sha_hao_fen_xi.html`
- 在"后区推荐杀号"区域（第108行后）添加"查看回测结果"链接
- 更新链接的 `onclick` 事件，使其传递 `backtest_period=50` 和当前选择的统计范围作为 `stats_period`

## 3. 实现后端接口 `huo_qu_sha_hao_hou_hui_ce`
- 创建新的后端文件 `hou/huo_qu_sha_hao_hou_hui_ce.js`
- 实现 `huo_qu_sha_hao_hou_hui_ce` 函数，使用与sha_hao_fen_xi.html页相同的后区推荐杀号算法
- 函数接受 `backtest_period` 和 `stats_period` 两个参数
- 实现回测逻辑，比较推荐杀号与真实开奖结果
- 返回包含回测结果的JSON数据
- 在 `server.js` 中注册该路由

## 4. 测试
- 确保从 `sha_hao_fen_xi.html` 点击"查看回测结果"能正确跳转到后区回测详情页
- 确保后区回测详情页的下拉框能正确影响数据加载
- 确保回测期数改变后，回测结果列表数据跟着改变
- 确保统计期数改变后，后区推荐杀号算法跟着改变

这个计划将确保所有功能都按照用户要求实现，并且保持与之前修改的其他回测页面的一致性。