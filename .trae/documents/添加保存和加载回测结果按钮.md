### 实现计划

1. **添加按钮HTML结构**
   - 在"总回测控制"卡片的"复制推荐买号"按钮右侧添加两个新按钮
   - 按钮1："保存回测结果"，ID为`saveBacktestResultsBtn`
   - 按钮2："加载回测结果"，ID为`loadBacktestResultsBtn`

2. **实现保存回测结果功能**
   - 新增`bao_cun_hui_ce_jie_guo_quan_bu`函数，用于收集所有回测结果
   - 该函数需要：
     - 获取当前最新期号
     - 收集所有卡片的回测结果数据
     - 为每个卡片生成唯一的cacheKey
     - 调用现有的`bao_cun_hui_ce_jie_guo`函数保存到数据库
     - 显示保存成功/失败的提示

3. **实现加载回测结果功能**
   - 新增`jia_zai_hui_ce_jie_guo`函数，用于从数据库加载回测结果
   - 该函数需要：
     - 获取当前最新期号
     - 为每个卡片生成唯一的cacheKey
     - 调用现有的`huo_qu_hui_ce_jie_guo`函数获取数据
     - 调用对应的渲染函数将数据回显到页面
     - 显示加载成功/失败的提示

4. **添加事件监听器**
   - 为新增的两个按钮添加点击事件监听器
   - 将按钮与对应的功能函数连接起来

5. **实现回测结果收集功能**
   - 新增`collectAllBacktestResults`函数，用于收集所有卡片的回测结果
   - 该函数需要遍历所有卡片的表格，提取回测结果数据

### 技术实现要点

1. **cacheKey生成规则**：使用卡片名称、当前期号、回测期数等信息生成唯一的cacheKey
2. **数据结构设计**：确保收集的回测结果数据结构与现有的渲染函数兼容
3. **用户体验**：添加适当的提示信息，告知用户操作结果
4. **错误处理**：处理可能的网络错误和数据格式错误
5. **代码复用**：尽量复用现有的函数和数据结构

### 预期效果

- 用户点击"保存回测结果"按钮后，系统会将当前页面的所有回测结果保存到数据库
- 用户点击"加载回测结果"按钮后，系统会根据当前期号从数据库加载对应的回测结果并显示在页面上
- 操作结果会通过提示信息反馈给用户

### 实现步骤

1. 修改HTML，添加两个新按钮
2. 实现`collectAllBacktestResults`函数
3. 实现`bao_cun_hui_ce_jie_guo_quan_bu`函数
4. 实现`jia_zai_hui_ce_jie_guo`函数
5. 添加按钮点击事件监听器
6. 测试功能是否正常工作