# 移除旧回测方法的处理逻辑

## 问题分析
用户询问为什么 `xia_qi_hou_qu_kai_jiang_yu_ce.html` 页面的几个卡片还没有把回测方法：出现最多、出现最少、出现平均的算法去掉。

经过代码分析，我发现：
1. `performSearchNewestBack()`、`performSearchSecondLastBack()` 和 `performSearchThirdLastBack()` 函数只包含排名方法（rank1到rank12）
2. 但是在渲染函数和获取推荐买号的函数中，仍然保留了对旧方法的处理逻辑

## 解决方案

### 需要修改的文件：
`d:\中啦\caishen4\qian\js\xia_qi_hou_qu_kai_jiang_yu_ce.js`

### 需要修改的函数：

1. **`renderDetailDataSecondLastBack()` 函数**（行970-1052）
   - 移除对 'least' 和 'average' 方法的中文名称处理

2. **`renderDetailDataThirdLastBack()` 函数**（行1054-1136）
   - 移除对 'least' 和 'average' 方法的中文名称处理

3. **`huo_qu_hou_qu_tui_jian_mai_hao()` 函数**（行263-373）
   - 移除对 'most'、'least' 和 'average' 方法的处理逻辑
   - 只保留对排名方法的处理

4. **`huo_qu_hou_qu_tui_jian_mai_hao_second_last()` 函数**（行1503-1576）
   - 移除对 'most'、'least' 和 'average' 方法的处理逻辑
   - 添加对排名方法的处理

5. **`huo_qu_hou_qu_tui_jian_mai_hao_third_last()` 函数**（行1579-1683）
   - 移除对 'most'、'least' 和 'average' 方法的处理逻辑

### 修改步骤：
1. 首先修改 `renderDetailDataSecondLastBack()` 函数
2. 然后修改 `renderDetailDataThirdLastBack()` 函数
3. 接着修改 `huo_qu_hou_qu_tui_jian_mai_hao()` 函数
4. 再修改 `huo_qu_hou_qu_tui_jian_mai_hao_second_last()` 函数
5. 最后修改 `huo_qu_hou_qu_tui_jian_mai_hao_third_last()` 函数

### 验证：
修改完成后，确保所有卡片只显示排名方法的回测结果，不再显示出现最多、出现最少、出现平均的结果。