我需要修改所有页面中图表绘制函数的统计期数显示位置，将其从右上角移动到左侧靠近Y轴处。具体修改步骤如下：

1. **修改所有页面的图表绘制函数**：
   - 找到所有页面中的 `drawFrontZoneChart` 函数
   - 找到所有页面中的 `drawBackZoneChart` 函数（如果存在）

2. **调整统计期数显示的代码**：
   - 将 `ctx.textAlign` 从 'right' 改为 'left'
   - 将 X 坐标从 `chartWidth - padding.right` 改为 `padding.left + 5`（靠近Y轴）
   - 保持 Y 坐标不变（`padding.top + 15`）

3. **修改的文件包括**：
   - sha_hao_fen_xi.html
   - dao_shu_2_qi_liang_qiu_zu_he.html
   - dao_shu_3_qi_liang_qiu_zu_he.html
   - san_qiu_fen_xi.html
   - dao_shu_2_qi_san_qiu_zu_he.html
   - dao_shu_3_qi_san_qiu_zu_he.html
   - dao_shu_4_qi_liang_qiu_zu_he.html
   - dao_shu_5_qi_liang_qiu_zu_he.html

4. **验证修改效果**：
   - 确保所有图表的统计期数显示在左侧靠近Y轴处
   - 文字清晰可读，不与其他图表元素重叠

通过这些修改，所有图表的统计期数信息将从右上角移动到左侧靠近Y轴的位置，符合用户要求。