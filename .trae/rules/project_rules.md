默认中文
当接口失败时，永远不要使用模拟数据，直接弹窗提示“接口失败”
任务完成后自动刷新页面
接口命名用中文拼音,可以中间加_
前端函数命名要用中文拼音，可以中间加_
每个接口都生成一个独立文件，方便使用
前端统一使用端口8080
后端统一使用端口18889
任务完成后自动删除测试的文件，不需要人工删除
每新增或修改一个前端页面，就要在“文档说明.md”中把这个页面的核心功能补充上，包括页面的导航栏、页面的功能、页面的样式等。
每新增或修改一个后端接口，就要在“文档说明.md”中把这个接口的核心功能补充上，包括接口的参数、接口的返回值、接口的使用示例等。
代码中绝对不能有乱码