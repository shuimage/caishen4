# 下期后区深度预测回测逻辑修改 - 实现计划

## [ ] 任务 1：分析开奖预测页面的实现逻辑
- **优先级**：P0
- **依赖**：无
- **描述**：
  - 分析 xia_qi_hou_qu_kai_jiang_yu_ce.html 页面的"倒数2期两球组合后区平均率最高统计期详情"的实现逻辑。
  - 重点分析 performSearchSecondLastBack 函数和 renderDetailDataSecondLastBack 函数的实现。
  - 分析后区推荐买号合集的生成逻辑。
- **验收标准**：AC-1
- **测试要求**：
  - `human-judgment` TR-1.1：理解搜索策略的实现细节。
  - `human-judgment` TR-1.2：理解推荐买号合集的生成逻辑。
- **备注**：重点关注搜索策略的阶段设置和推荐买号合集的排序逻辑。

## [ ] 任务 2：修改深度预测页面的回测逻辑
- **优先级**：P0
- **依赖**：任务 1
- **描述**：
  - 修改 xia_qi_hou_qu_shen_du_yu_ce.html 页面的 executeCardBacktest 函数，使其在处理倒数2期卡片时使用与开奖预测页面相同的搜索策略。
  - 确保使用与开奖预测页面相同的API调用和参数。
  - 确保使用与开奖预测页面相同的推荐买号获取函数。
- **验收标准**：AC-2
- **测试要求**：
  - `programmatic` TR-2.1：深度预测页面的倒数2期回测使用与开奖预测页面相同的搜索策略。
  - `programmatic` TR-2.2：深度预测页面的倒数2期回测使用与开奖预测页面相同的API调用。
- **备注**：确保搜索策略的阶段设置、步长和扩展范围与开奖预测页面一致。

## [ ] 任务 3：修改深度预测页面的推荐买号合集提取逻辑
- **优先级**：P0
- **依赖**：任务 2
- **描述**：
  - 修改 xia_qi_hou_qu_shen_du_yu_ce.html 页面的 extractBuyNumbersCollection 函数，使其使用与开奖预测页面相同的逻辑。
  - 确保正确计算第1名和最后1名。
- **验收标准**：AC-3
- **测试要求**：
  - `programmatic` TR-3.1：深度预测页面的推荐买号合集提取逻辑与开奖预测页面一致。
  - `programmatic` TR-3.2：回测结果卡片的"回测结果值"正确显示第1名和最后1名。
- **备注**：确保按照正确率累加排序，并正确处理相同正确率的情况。

## [ ] 任务 4：测试修改后的功能
- **优先级**：P1
- **依赖**：任务 3
- **描述**：
  - 在深度预测页面执行回测，验证修改后的功能是否正确。
  - 与开奖预测页面的结果进行对比，确保一致性。
- **验收标准**：AC-2, AC-3
- **测试要求**：
  - `programmatic` TR-4.1：深度预测页面的回测结果与开奖预测页面一致。
  - `human-judgment` TR-4.2：回测结果卡片的"回测结果值"正确显示第1名和最后1名。
- **备注**：测试不同期数的回测，确保功能的稳定性。